# 13CIT TabManager

## run app

``` pip install -r requirements.txt ```
``` python main.py ```

## or through venv

``` python -m venv .venv ```
``` source .venv/bin/activate ```
``` pip install -r requirements.txt ```
``` python main.py ```